# Cae

基于 Koa 的 MVC 框架

#安装

- npm

```shell
  npm install --save cae
```

- yarn：

```shell
  yarn add cae
```

#使用

```javascript
'use strict';

const { Cae } = require('../index');

const app = new Cae();
app.run();
```
